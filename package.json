{
  "name": "imgponic-pwa",
  "version": "0.1.4",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "pnpm -s eslint \"{src,tests}/**/*.{ts,tsx}\"",
    "typecheck": "pnpm -s tsc --noEmit",
    "test": "pnpm run tsx scripts/test/run-selected.ts",
    "test:ci": "pnpm test -- --filter ci",
    "test:autosave": "pnpm test -- --filter autosave",
    "test:merge": "pnpm test -- --filter merge",
    "test:cli": "pnpm test -- --filter cli",
    "test:collector": "pnpm test -- --filter collector",
    "test:telemetry": "pnpm test -- --filter telemetry",
    "test:golden": "pnpm test -- --filter golden",
    "test:coverage": "NODE_OPTIONS=\"--import tsx/esm${NODE_OPTIONS:+ $NODE_OPTIONS}\" node --input-type=module --eval \"import { mkdirSync } from 'node:fs'; const dir = process.argv.at(-1); if (!dir) throw new Error('missing dir'); mkdirSync(dir, { recursive: true });\" coverage && NODE_V8_COVERAGE=coverage NODE_OPTIONS=\"--import tsx/esm${NODE_OPTIONS:+ $NODE_OPTIONS}\" node --input-type=module --eval \"import { spawnSync } from 'node:child_process'; import { readdirSync, statSync } from 'node:fs'; import { join } from 'node:path'; import { pathToFileURL } from 'node:url'; const coverageFlag = '--test-coverage-dir=coverage'; const [major] = process.versions.node.split('.', 1); const coverageArgs = Number.parseInt(major, 10) >= 22 ? ['--test-coverage', coverageFlag] : []; const { DEFAULT_TEST_SUFFIXES } = await import(pathToFileURL(join(process.cwd(), 'scripts/test/run-selected.ts')).href); const defaultSuffixes = [...DEFAULT_TEST_SUFFIXES]; const files = []; const walk = (dirPath) => { for (const entry of readdirSync(dirPath)) { const full = join(dirPath, entry); const stat = statSync(full); if (stat.isDirectory()) { walk(full); } else if (defaultSuffixes.some((suffix) => full.endsWith(suffix))) { files.push(full); } } }; walk('tests'); const result = spawnSync(process.execPath, ['--experimental-vm-modules', '--import', 'tsx/esm', '--experimental-specifier-resolution=node', '--test', ...coverageArgs, ...files], { stdio: 'inherit', env: process.env }); if (result.error) throw result.error; process.exit(result.status ?? 0);\" -- --test-coverage-dir=coverage",
    "test:junit": "NODE_OPTIONS=\"--import tsx/esm${NODE_OPTIONS:+ $NODE_OPTIONS}\" node --input-type=module --eval \"import { existsSync, mkdirSync, readdirSync, renameSync, rmSync, statSync } from 'node:fs'; import { join } from 'node:path'; import { pathToFileURL } from 'node:url'; import { spawnSync } from 'node:child_process'; const extraArgs = process.argv.slice(1); mkdirSync('reports', { recursive: true }); mkdirSync('file=reports', { recursive: true }); const { DEFAULT_TEST_SUFFIXES } = await import(pathToFileURL(join(process.cwd(), 'scripts/test/run-selected.ts')).href); const defaultSuffixes = [...DEFAULT_TEST_SUFFIXES]; const tests = []; const walk = (dir) => { for (const entry of readdirSync(dir)) { const full = join(dir, entry); const stat = statSync(full); if (stat.isDirectory()) { walk(full); } else if (defaultSuffixes.some((suffix) => full.endsWith(suffix))) { tests.push(full); } } }; walk('tests'); const args = ['--experimental-vm-modules', '--import', 'tsx/esm', '--experimental-specifier-resolution=node', '--test', '--test-reporter=junit', '--test-reporter-destination=file=reports/junit.xml', ...extraArgs, ...tests]; const result = spawnSync(process.execPath, args, { stdio: 'inherit', env: process.env }); if (existsSync('file=reports/junit.xml')) { renameSync('file=reports/junit.xml', 'reports/junit.xml'); const entries = readdirSync('file=reports'); if (entries.length === 0) { rmSync('file=reports', { recursive: true }); } } if (result.error) throw result.error; process.exit(result.status ?? 0);\""
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zustand": "^4.5.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.38.0",
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "@typescript-eslint/eslint-plugin": "^8.10.0",
    "@typescript-eslint/parser": "^8.10.0",
    "@vitejs/plugin-react": "^4.3.1",
    "eslint": "^9.11.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3",
    "vite": "^5.4.8",
    "vitest": "^4.0.8",
    "arg": "^6.0.0"
  },
  "eslintConfig": {
    "root": true,
    "env": {
      "browser": true,
      "es2022": true,
      "node": true
    },
    "parser": "@typescript-eslint/parser",
    "parserOptions": {
      "ecmaVersion": "latest",
      "sourceType": "module"
    },
    "plugins": [
      "@typescript-eslint"
    ],
    "extends": [
      "eslint:recommended",
      "plugin:@typescript-eslint/recommended"
    ]
  }
}
