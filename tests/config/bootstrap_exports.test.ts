import { test } from 'node:test'

test.skip('bootstrap_exports: functions are exported', () => {
  // Skipped - module does not exist
})

test.skip('bootstrap_exports: produce plan objects', () => {
  // Skipped - module does not exist
})
